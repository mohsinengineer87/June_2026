# PASTA-ML — dependencies pinned to versions with pre-built wheels for
# Python 3.11, 3.12, 3.13, and 3.14 (Streamlit Cloud may use any of these).
#
# Key minimum version constraints (lower versions lack cp311/cp314 wheels):
#   numpy  >= 2.0.0  (1.x has no cp314 wheels)
#   pandas >= 3.0.0  (2.x has no cp314 wheels)
#   scikit-learn >= 1.6.0  (1.5.x has no cp314 wheels)
#   shap >= 0.51.0  (0.51 has cp311 wheels; 0.52+ uses abi3 for 3.12+)

streamlit>=1.45.0
pandas>=3.0.0
numpy>=2.0.0
scikit-learn>=1.6.0
networkx>=3.3
plotly>=5.24.0
shap>=0.51.0
joblib>=1.4.2
python-docx>=1.1.0
